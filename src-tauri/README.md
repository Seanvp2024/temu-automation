# Legacy Tauri Workspace

This folder is kept as legacy migration work and reference code.

It is not the active desktop runtime for this repository.

Current active path:

- UI: `src/`
- Desktop shell: `electron/`
- Automation runtime: `automation/worker.mjs`

Do not add new feature work here unless we explicitly decide to restart a Tauri migration.
